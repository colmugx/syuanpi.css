@keyframes dead
    from
        opacity: 1
        transform(scale(1,1))
    60%
        opacity: 0.9
        transform(scale(1.2,1.2))
    to
        opacity: 0
        transform(scale(0.8,0.8))

.dead
    animation-name: dead
    animation-timing-function: ease-out
