@keyframes bloom
    from
        opacity: 0
        transform(scale(0.8,0.8))
    75%
        transform(scale(1.1,1.1))
    to
        opacity: 1
        transform(scale(1.0,1.0))

.bloom
    animation-name: bloom
